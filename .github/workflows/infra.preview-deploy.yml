name: "infra preview & deploy"

on:
  push:
    branches:
      - main
    paths:
      - 10_infra/**
      - .github/workflows/**
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  preview:
    name: "Preview"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello world"
